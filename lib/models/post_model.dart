class PostModel {
  String? postId, postTitle, postImageUrl, postDescription, time;

  PostModel({
    this.postId,
    this.postTitle,
    this.postImageUrl,
    this.postDescription,
    this.time,
  });
}
